<UserControl x:Class="LearnHub.App.Views.PlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="Margin" Value="0,0,8,0" />
                </Style>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="0,0,0,0" />
                </Style>
            </StackPanel.Resources>
            <TextBox Width="400" Text="{Binding Prompt, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Generate" Command="{Binding GenerateCommand}" />
        </StackPanel>
        <ScrollViewer Grid.Row="1" Margin="0,12,0,0">
            <StackPanel>
                <TextBlock Text="{Binding Plan.PlanTitle}" FontSize="20" FontWeight="SemiBold" />
                <ItemsControl ItemsSource="{Binding Plan.Prerequisites}" Margin="0,8,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="• {Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ItemsControl ItemsSource="{Binding Plan.WeeklySchedule}" Margin="0,12,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,4,0,4">
                                <TextBlock Text="{Binding WeekLabel}" FontWeight="SemiBold" />
                                <ItemsControl ItemsSource="{Binding Days}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="0,2,0,2">
                                                <TextBlock Text="{Binding DayLabel}" FontWeight="SemiBold" />
                                                <TextBlock Text="Reading:" FontWeight="Bold" />
                                                <ItemsControl ItemsSource="{Binding Reading}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate><TextBlock Text="- {Binding}" /></DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <TextBlock Text="Watching:" FontWeight="Bold" />
                                                <ItemsControl ItemsSource="{Binding Watching}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate><TextBlock Text="- {Binding}" /></DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <TextBlock Text="Practice:" FontWeight="Bold" />
                                                <ItemsControl ItemsSource="{Binding PracticeTasks}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate><TextBlock Text="- {Binding}" /></DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <TextBlock Text="Checkpoints:" FontWeight="Bold" />
                                                <ItemsControl ItemsSource="{Binding Checkpoints}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate><TextBlock Text="- {Binding}" /></DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
