<UserControl x:Class="LearnHub.App.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="16">
        <TextBlock Text="Library" FontSize="20" FontWeight="SemiBold" />
        <ListView ItemsSource="{Binding Downloads}" Margin="0,8,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Video" DisplayMemberBinding="{Binding VideoId}" Width="200" />
                    <GridViewColumn Header="Path" DisplayMemberBinding="{Binding FilePath}" Width="320" />
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="120" />
                    <GridViewColumn Header="Timestamp" DisplayMemberBinding="{Binding Timestamp}" Width="180" />
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
